You have full access to the GitHub repository at https://github.com/K1ngs1/kepler.

## THE PROBLEM
When a user navigates to the trade proposal form (accessible from /auctions) and selects cards to offer and request, the **"Propose Trade" button does nothing**. No trade is created in the database. No error is shown. The button is non-functional.

The screenshot shows:
- Trading with: pandeyaryan7777
- Your Cards to Offer: Pikachu (Base Set #58) — selected
- Their Cards to Request: Charizard (Base Set #4) — selected
- Clicking "Propose Trade" produces no result

## WHAT ALREADY EXISTS IN THE REPOSITORY
- Supabase is configured and connected.
- The PostgreSQL function `propose_trade(p_recipient_id uuid, p_offered_card_ids uuid[], p_requested_card_ids uuid[])` exists in the database and works correctly when called directly.
- The database schema has all required tables: `user_cards`, `trade_offers`, `trade_items`, `profiles`, `catalog_cards`.
- The user has cards in their collection marked `for_trade = true`.
- The recipient (pandeyaryan7777) has cards marked `for_trade = true`.

## YOUR MISSION
Find and fix the broken "Propose Trade" button. You must identify why the click handler fails and make the button fully functional.

## STEP-BY-STEP DIAGNOSIS AND FIX

### 1. Locate the trade proposal component
Search for the component that renders the trade proposal form. Look for:
- "Trading with:" text
- "Propose Trade" button text
- `propose_trade` function calls
- References to `supabase.rpc`

### 2. Identify the click handler
Find the `onClick` handler attached to the "Propose Trade" button. Check:
- Does it call `supabase.rpc('propose_trade', ...)`?
- Are the arguments correct? The function expects:
  - `p_recipient_id` (uuid of the recipient user)
  - `p_offered_card_ids` (array of `user_cards.id` values)
  - `p_requested_card_ids` (array of `user_cards.id` values)
- Are the card IDs being collected correctly from the selected cards?
- Is the button disabled or enabled based on selection state?

### 3. Check for common issues
- **Missing Supabase client**: The component may not have access to the initialized Supabase client.
- **Wrong function name**: The function in the database is `propose_trade`, not `create_trade` or `new_trade`.
- **Incorrect parameter names**: The parameters must be named `p_recipient_id`, `p_offered_card_ids`, `p_requested_card_ids`.
- **Missing authentication**: The user must be logged in for `auth.uid()` to work inside the function.
- **Empty arrays**: The button might be clicked but the selected card IDs might not be properly stored in state.

### 4. Add error handling and user feedback
Even if the function call fails, the button should show an error. Add:
- A `try/catch` block around the RPC call.
- Display any error message from the database (e.g., "Card is not available for trade") via a toast or alert.
- Show a loading state while the trade is being created.
- Redirect to the trade detail page on success.

### 5. Verify the implementation
After your fix, the following must work:
1. User selects cards to offer and request.
2. User clicks "Propose Trade".
3. A loading spinner appears on the button.
4. The trade is created in the database (a new row in `trade_offers`).
5. The user is redirected to the trade detail page or shown a success message.
6. If an error occurs (e.g., card no longer available), a clear error message is shown.

## CRITICAL CONSTRAINTS
1. **DO NOT CHANGE THE UI**. Keep all existing colors, fonts, spacing, and layout exactly as they are.
2. Use the existing Supabase client configuration.
3. Call the PostgreSQL function via `supabase.rpc()`, never insert directly into `trade_offers`.
4. The function signature must match exactly: `propose_trade(p_recipient_id, p_offered_card_ids, p_requested_card_ids)`.
5. `p_offered_card_ids` and `p_requested_card_ids` must be arrays of `user_cards.id` (UUIDs), NOT `catalog_cards.id`.

## DELIVERABLES
- The fixed trade proposal component with working "Propose Trade" button.
- Any additional utility functions or hooks needed.
- A brief explanation of what was broken and how you fixed it.
- No changes to existing CSS or visual styling.

## HOW TO TEST
1. Log in as a user who has cards marked `for_trade = true`.
2. Navigate to the trade proposal form for another user who has tradeable cards.
3. Select cards to offer and request.
4. Click "Propose Trade".
5. Verify that a new trade appears in the `trade_offers` table and the user sees a success confirmation.

Now diagnose the issue step by step, fix the broken button, and ensure the trade proposal flow works end-to-end.