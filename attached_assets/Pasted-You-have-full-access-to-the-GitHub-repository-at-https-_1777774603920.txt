You have full access to the GitHub repository at https://github.com/K1ngs1/kepler.

## WHAT EXISTS – DO NOT TOUCH OR REBUILD
- Supabase project connected (credentials live in `.replit`).  Supabase URL:
  `https://yquwasetajootlgmyxan.supabase.co`
- **Database schema** `supabase/migrations/001_schema.sql` already has:
  * `profiles` (auto‑populated on first sign‑up)
  * `catalog_cards` (id, tcgdex_id, name, set_name, set_code, number, rarity, image_url)
  * `user_cards` (id, user_id, catalog_card_id, condition, quantity, for_trade, wanted, notes)
  * `trade_offers`, `trade_items`, `lots`, `bids`
  * RLS policies that allow owners full access and let everyone see trade‑listed cards
- **Trade functions** `supabase/migrations/002_trade_functions.sql`:
  * `trade_messages` table (realtime enabled)
  * `propose_trade`, `accept_trade`, `counter_offer`, `cancel_trade`, `complete_trade`
  * `initiator_confirmed` / `recipient_confirmed` flags on `trade_offers`
- **Seed script** `scripts/seed-catalog.mjs` – populates `catalog_cards` from the free TCGdex API
- **Backend** `backend/data.py` & `backend/main.py` – FastAPI server serving **hardcoded mock data** 
  (HERO_SLIDES, AUCTIONS_DATA, LOTS_DATA, etc.).  These files still exist but are irrelevant for the trading app.
- **Frontend** lives in a `frontend/` directory.  
  The Replit workflow runs `cd frontend && npm run dev -- --hostname 0.0.0.0 --port 5000`.

## YOUR MISSION
Every single page in the Kepler frontend must pull **real user‑specific data from Supabase**.
Right now some pages still display hardcoded fake content (example names, placeholder cards,
static counts).  You must systematically replace ALL mock data with live database queries.

**For example:** the user has already added a Charizard to their collection and marked it
`for_trade = true`.  After your changes, navigating to the “For Trade” / “My Collection”
page MUST show that exact Charizard (and any other cards the user owns).

## CRITICAL RULE – DO NOT CHANGE THE UI
The visual design is already built and must remain pixel‑perfect.  Only change the **source
of the data** inside the components, not the HTML, CSS, or styling.  Keep every class,
colour, font, spacing, shadow, and layout exactly as it is – just swap out the fake
arrays/objects for real Supabase fetches.

## IDENTIFYING FAKE DATA
Open every page component file and look for:
* Hardcoded arrays (e.g. `const trades = [...]`, `const cards = [...]`)
* Static imports of mock data
* Placeholder text like "Charizard", "Pikachu", "Trainer123"
* Empty states that say "No data yet" without attempting a database query

Mark those locations – they need real queries.

## REQUIRED REAL‑DATA CONNECTIONS

### 1. Collection Page (My Cards / Binder)
Must display the **current authenticated user’s** `user_cards` joined with `catalog_cards`:
supabase
.from('user_cards')
.select(', catalog_cards()')
.eq('user_id', auth.user.id)
Show card image, name, set, condition, quantity, and toggles for `for_trade`/`wanted`.
If the user has a Charizard, it must appear here.

### 2. For Trade / Tradeable Cards Page
Must show only cards where `for_trade = true` for the logged‑in user:
supabase
.from('user_cards')
.select(', catalog_cards()')
.eq('user_id', auth.user.id)
.eq('for_trade', true)

### 3. Trade List (/trades)
Fetch trades where the current user is initiator OR recipient.  The current schema
has `trade_offers` with `initiator_id` and `recipient_id`.  Use the correct column
names from the migration.

Join `trade_items` so you can display the cards involved.  Subscribe to realtime
changes on `trade_offers` so new offers appear live.

### 4. Trade Detail (/trades/[id])
Load the specific trade by ID.  Show the real cards offered/requested from
`trade_items`.  The chat panel must load existing `trade_messages` ordered by
`sent_at` and subscribe to new messages via Supabase Realtime.

Action buttons (Accept, Counter, Cancel, Confirm Receipt) must call the
PostgreSQL functions that already exist:
* `accept_trade(trade_id)`
* `counter_offer(trade_id, offered_card_ids, requested_card_ids)`
* `cancel_trade(trade_id)`
* `complete_trade(trade_id)`

Call them via `supabase.rpc()`.

### 5. Wishlist / Wanted Cards
Show the user’s `user_cards` where `wanted = true`.  Optionally show matches:
other users who have those cards `for_trade = true`.

### 6. Catalog (/catalog)
Fetch from `catalog_cards` (already seeded via the script).  Add to Collection
must insert into `user_cards`.

### 7. Homepage
If the homepage currently shows hardcoded slides/auctions, replace them with:
* Hero carousel: 5 most recent `catalog_cards` (or "Coming soon" for now).
* "Hot Cards" section: cards with highest count in `user_cards`.
* "Recent Completed Trades": last 5 trades with status `'completed'`.

If these queries are too heavy for the MVP, at minimum remove all static
arrays and show a message like "X cards in catalog" where X comes from a
real `catalog_cards` count.

## DATABASE FUNCTIONS ALREADY EXIST
Do NOT recreate the trade functions.  They already live in
`supabase/migrations/002_trade_functions.sql` and accept these signatures:

* `propose_trade(p_recipient_id uuid, p_offered_card_ids uuid[], p_requested_card_ids uuid[])` returns uuid
* `accept_trade(p_trade_id uuid)` returns void
* `counter_offer(p_trade_id uuid, p_offered_card_ids uuid[], p_requested_card_ids uuid[])` returns void
* `cancel_trade(p_trade_id uuid)` returns void
* `complete_trade(p_trade_id uuid)` returns void

All are `SECURITY DEFINER` and call `auth.uid()` internally.

Call them exactly as they are defined – e.g.:
```ts
await supabase.rpc('accept_trade', { p_trade_id: tradeId })
TABLE/COLUMN NAMES – USE EXACTLY THESE
The schema already defines:

catalog_cards (id, tcgdex_id, name, set_name, set_code, number, rarity, image_url)

user_cards (id, user_id, catalog_card_id, condition, quantity, for_trade, wanted, notes)

trade_offers (id, initiator_id, recipient_id, status, message, initiator_confirmed, recipient_confirmed, created_at, updated_at)

trade_items (id, trade_id, user_card_id, direction) – direction is 'offer' or 'request'

trade_messages (id, trade_id, sender_id, content, sent_at)

profiles (id, username, avatar_url, location, reputation_score)

Use the exact column names from the migration files. Do NOT rename anything.

SUPABASE CLIENT
The Supabase client is used directly from the frontend:

import { createClient } from '@supabase/supabase-js'

Environment variables: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY

The .replit file already has these set in [userenv.shared]

HOW TO VERIFY YOUR WORK
After making changes, the following must be true:

Log in as a user → go to "For Trade" page → see the user's own Charizard
(or other cards they've marked) from the database.

Navigate to each page → no obviously fake content (no "Placeholder Card",
no "User123", no hardcoded auction titles).

Open the browser console → no errors about missing data.

DELIVERABLES
Modified page components (TypeScript) that now fetch real data from Supabase.

Any additional hooks or utilities needed for realtime subscriptions or
data fetching (keep them in lib/ or hooks/).

No changes to CSS files or visual styling – the UI must look identical
to how it looks now.

A summary in the PR/reply listing every file changed and what mock data
was removed.

CONSTRAINTS
All data access must use the Supabase client.

RLS policies are already in place – make sure your queries filter by the
user’s ID as needed; do not bypass RLS.

Keep everything within Supabase free tier limits; use limit() where appropriate.

Never change the UI styling or component structure – only replace the
data source.

Do NOT delete or modify backend/ files.

Now, step through the codebase systematically, remove every trace of fake data,
and wire up real Supabase data across the entire app. The pages must show the
real cards the user has in their collection – including that Charizard.


---

This refined prompt now:
* References the **exact table and column names** from your actual migration files (like `catalog_cards`, `trade_items.direction`, etc.)
* Lists the **existing trade functions** by their real signatures (`propose_trade`, `accept_trade`, etc.) so the agent doesn't recreate them
* Tells the agent exactly which Supabase URL and anon key are available in `.replit`
* Prevents any changes to the UI, backend files, or the database schema
* Gives specific queries the agent should use for each page



